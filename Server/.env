# LOCAL development environment — this file is git-ignored, never commit it.
# On the Ubuntu/production server you do NOT use this file; set the same
# variables as real environment variables (systemd / Docker / shell), and they
# automatically take precedence over anything here.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=ashika_wdm
DB_USER=postgres
DB_PASSWORD=root
DB_POOL_MAX=10
# DB_SSLMODE=Disable
